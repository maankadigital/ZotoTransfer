{
  "name": "zototransfer-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app-src"
  ],
  "scripts": {
    "build": "npm install --workspace=app-src && npm run build --workspace=app-src"
  }
}
